# Vendored: libxposed API (API 100)

来源:https://github.com/libxposed/api (tag `100`)
许可证:Apache-2.0(见各源文件头部声明)

仅用于编译期类型检查;运行时由 LSPosed/Xposed 框架注入同名实现。
选择 API 100 是为了兼容存量 LSPosed 版本,Maven Central 上该版本无发布包。
